#include <stdio.h>


/**
 *main - Entry Point
 *
 *Return: Always 0 (Success)
 */

int main() {
    int num;

    for (num = 10; num >= 0; num--)
      printf("%d", num);
    putchar('\n');

    return 0;
}
