#include "main.h"
#include <string.h>



char *_strstr(char *haystack, char *needle)
{
  unsigned int i, j;
  unsigned int length_needle = strlen(needle);

  for (i = 0; haystack[i] != '\0'; ++i)
    {
      
    }
}
